services:

  _defaults:
    autowire: true
    autoconfigure: true

  App\EventSystem\Response\EventListener\EtagResponseListener:
    tags:
      - name: kernel.event_listener
        event: 'kernel.response'
        method: 'onKernelResponse'
